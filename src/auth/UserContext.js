import React from "react";

/** User context for navigation, homepage, and job application button. */
const UserContext = React.createContext();
console.debug("UserContext", UserContext)

export default UserContext;